import { expect } from "chai";
import * as sinon from "sinon";


describe("comment.ts", () => {

    afterEach(() => {
        sinon.restore();
    });

    describe("", () => {
        it("should...", async () => {
            expect(false).to.equal(true);
          });
    })

});